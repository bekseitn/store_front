# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html
#
# order_status_id was removed here: it belongs to the `orderings` table
# (see orderings.yml), not `orders` - orders never had that column.
# This fixture never actually loaded successfully before (found by
# finally running the suite for real against Rails 7.2).

one:
  total: 1

two:
  total: 1
